import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const source=fs.readFileSync(path.resolve(import.meta.dirname,"../components/opportunities/live-opportunity-universe.tsx"),"utf8");assert.match(source,/sortOpportunitiesForExecutive/);assert.match(source,/activeOpportunities\.slice\(safePage \* pageSize/);assert.match(source,/Next \{view === "Recommended" \? "recommendations"/);assert.match(source,/lower-confidence matches/);assert.doesNotMatch(source,/filter\(.*\)\.slice\(0, 12\)/s);console.log("Recommendation pagination checks passed.");
