import type { CompensationRecord } from "@/types/compensation";
export const annualizeMonthly=(value:number)=>value*12; export const rangeMidpoint=(min?:number,max?:number)=>min!==undefined&&max!==undefined?(min+max)/2:undefined;
export function estimateTotalCash(r:CompensationRecord){const base=r.baseSalaryExact??rangeMidpoint(r.baseSalaryMin,r.baseSalaryMax);const variable=r.variableCompensationExact??rangeMidpoint(r.variableCompensationMin,r.variableCompensationMax)??(base&&r.variableCompensationPercent?base*r.variableCompensationPercent/100:0);return base===undefined?undefined:base+variable+(r.signingBonus??0)+(r.retentionBonus??0)}
export function estimateTotalCompensation(r:CompensationRecord){const cash=estimateTotalCash(r);return cash===undefined?undefined:cash+(r.equityValueEstimate??0)+(r.pensionValue??0)+(r.benefitsValueEstimate??0)}
export const difference=(a?:number,b?:number)=>a===undefined||b===undefined?undefined:a-b; export const isUndisclosed=(r:CompensationRecord)=>r.disclosedOrEstimated==="Undisclosed"||[r.baseSalaryExact,r.baseSalaryMin,r.baseSalaryMax].every(v=>v===undefined); export const isIncompletePackage=(r:CompensationRecord)=>isUndisclosed(r)||(!r.totalCompensationEstimate&&!r.variableCompensationExact&&!r.variableCompensationPercent);
