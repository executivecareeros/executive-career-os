import type {DataAccessMode,SupabaseConfiguration} from "./types";export interface DataAccessConfiguration{mode:DataAccessMode;supabase?:SupabaseConfiguration;errors:readonly string[];durable:boolean;rlsActive:boolean}export function resolveDataAccessConfiguration(environment:Readonly<Record<string,string|undefined>>):DataAccessConfiguration{const mode=environment.NEXT_PUBLIC_DATA_ACCESS_MODE==="supabase"?"supabase":"memory-demo";if(mode==="memory-demo")return{mode,errors:[],durable:false,rlsActive:false};const url=environment.NEXT_PUBLIC_SUPABASE_URL??environment.SUPABASE_URL,anonymousKey=environment.NEXT_PUBLIC_SUPABASE_ANON_KEY??environment.SUPABASE_ANON_KEY;const errors=[!url?"SUPABASE_URL_REQUIRED":"",!anonymousKey?"SUPABASE_ANON_KEY_REQUIRED":""].filter(Boolean);return{mode,errors,durable:errors.length===0,rlsActive:errors.length===0,supabase:url&&anonymousKey?{url,anonymousKey,mode:"supabase"}:undefined}}
export function requireSupabaseConfiguration(environment:Readonly<Record<string,string|undefined>>):SupabaseConfiguration{const configuration=resolveDataAccessConfiguration(environment);if(!configuration.supabase)throw new Error(`Supabase durable mode is not configured: ${configuration.errors.join(", ")||"configuration unavailable"}`);return configuration.supabase}
