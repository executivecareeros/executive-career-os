import type { AtlasConflict,AtlasContext,AtlasEvidence } from "./types";
export function detectAtlasConflicts(context:AtlasContext,evidence:readonly AtlasEvidence[]):AtlasConflict[]{const o=context.subject.opportunity,b=context.blueprint,result:AtlasConflict[]=[];const ids=(category:AtlasEvidence["category"])=>evidence.filter(e=>e.category===category).map(e=>e.id);if(o.salaryMax!==undefined&&o.salaryCurrency===b.compensation.currency&&o.salaryMax<b.compensation.minimum)result.push({id:"conflict-salary-minimum",type:"salary",severity:"critical",whyCode:"SALARY_BELOW_BLUEPRINT_MINIMUM",constraintId:"principle-pay",evidenceIds:ids("Compensation"),possibleResolutionCode:"VERIFY_OR_NEGOTIATE_TOTAL_COMPENSATION"});const travel=Number(o.travelRequirement.match(/\d+/)?.[0]);if(travel>b.travel.maximumPercent)result.push({id:"conflict-travel-limit",type:"travel",severity:"critical",whyCode:"TRAVEL_EXCEEDS_CONFIRMED_LIMIT",constraintId:"principle-travel",evidenceIds:ids("Opportunity"),possibleResolutionCode:"NEGOTIATE_TRAVEL_OR_REJECT"});for(const p of b.preferences.filter(p=>p.direction==="exclusion")){if(`${o.industry} ${o.country} ${o.jobTitle}`.toLowerCase().includes(p.value.toLowerCase()))result.push({id:`conflict-exclusion-${p.id}`,type:"industry",severity:p.requirement==="required"?"critical":"warning",whyCode:"BLUEPRINT_EXCLUSION_MATCHED",evidenceIds:ids("Executive Blueprint"),possibleResolutionCode:"VERIFY_CLASSIFICATION_OR_REJECT"});}if(o.missingRequirements.some(x=>x.toLowerCase().includes("language")))result.push({id:"conflict-language",type:"language",severity:"warning",whyCode:"LANGUAGE_REQUIREMENT_UNVERIFIED",evidenceIds:ids("Opportunity"),possibleResolutionCode:"CONFIRM_LANGUAGE_REQUIREMENT"});return result;}
