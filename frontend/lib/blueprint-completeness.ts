import { identifyBlueprintConflicts } from "./blueprint-conflicts"; import type { BlueprintCompleteness,BlueprintCompletenessState,ExecutiveBlueprint } from "@/types/executive-blueprint";
export function assessBlueprintCompleteness(b:ExecutiveBlueprint):BlueprintCompleteness{const required=[b.identity.headline,b.vision.threeYearObjective,b.vision.fiveYearObjective,b.preferences.length?"yes":"",b.principles.length?"yes":"",b.compensation.minimum?"yes":"",b.decisionPriorities.length?"yes":""];const completed=required.filter(Boolean).length;const sections=Object.entries(b.sectionStatus).map(([section,status])=>({section,status,requiredFields:1,completedFields:status==="empty"?0:1,confirmedAt:status==="confirmed"?b.lastConfirmedAt:undefined,evidenceCount:b.evidence.length}));const conflicts=identifyBlueprintConflicts(b);const score=Math.max(0,Math.round((completed/required.length)*70+(sections.filter(s=>s.status==="confirmed").length/sections.length)*20+Math.min(b.evidence.length*5,10)-conflicts.length*5));const state:BlueprintCompletenessState=score===0?"Not Started":score<45?"Basic":score<70?"Developing":score<90?"Strong":"Decision Ready";return{state,score,sections,factors:[`${completed} of ${required.length} required foundations completed`,`${sections.filter(s=>s.status==="confirmed").length} sections confirmed`,`${b.evidence.length} evidence references`,`${conflicts.length} unresolved conflicts`],missingRequired:required.map((v,i)=>v?"":`Required field ${i+1}`).filter(Boolean),staleSections:sections.filter(s=>s.status==="stale").map(s=>s.section),conflictCount:conflicts.length,assessedAt:"2026-07-11T12:00:00.000Z"};}
