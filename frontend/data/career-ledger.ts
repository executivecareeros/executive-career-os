import type { CareerLedgerEntry,LedgerEntityType,LedgerEventType,SerializableValue } from "@/types/career-ledger";import { applications } from "./applications";import { lifecycleEvents } from "@/lib/recruitment-lifecycle";
const make=(id:string,sequenceNumber:number,entityType:LedgerEntityType,entityId:string,eventType:LedgerEventType,title:string,newValue?:SerializableValue,occurredAt=`2026-07-${String(sequenceNumber+1).padStart(2,"0")}T10:00:00Z`):CareerLedgerEntry=>({id,sequenceNumber,occurredAt,recordedAt:occurredAt,entityType,entityId,eventType,title,summary:title,description:`Fictional append-only demonstration event: ${title}.`,actorType:eventType.includes("Agent")||eventType.includes("Recommendation")?"Agent":"User",actorId:eventType.includes("Agent")||eventType.includes("Recommendation")?"career-agent-demo":"demo-user",sourceType:"Demo Data",newValue,reason:"Demonstrate historical preservation.",evidenceReferences:["demo-evidence"],confidence:80,tags:[entityType.toLowerCase(),"demo"],relatedEntityIds:[entityId],documentVersionIds:[],compensationRecordIds:[],agentRunId:eventType.includes("Agent")||eventType.includes("Recommendation")?"demo-run-1":undefined,correlationId:`demo-workflow-${entityId}`,visibility:"Private",isArchived:false,isDemo:true});
const base:CareerLedgerEntry[]=[make("ledger-created",1,"Application","demo-application-northstar","Created","Application record created"),make("ledger-submitted",2,"Application","demo-application-northstar","Application Submitted","Demo application submitted"),make("ledger-asked",3,"Compensation","demo-application-northstar","Compensation Discussed","Candidate request recorded",250000),make("ledger-offer",4,"Offer","demo-application-northstar","Offer Received","Initial verbal offer recorded",225000),make("ledger-counter",5,"Compensation","demo-application-northstar","Counteroffer Made","Counteroffer preserved",245000),make("ledger-revised",6,"Offer","demo-application-northstar","Compensation Revised","Final package revised",343000),{...make("ledger-agent-old",7,"Agent","demo-northstar-cco","Recommendation Created","Atlas demo recommendation created","Apply"),isArchived:true,archiveReason:"Superseded, not deleted"},{...make("ledger-agent-revision",8,"Agent","demo-northstar-cco","Recommendation Revised","Atlas demo recommendation revised","Strong Apply"),previousValue:"Apply",correctsEntryId:"ledger-agent-old",causationId:"ledger-agent-old"}];
const lifecycleType=(key:string):LedgerEventType=>key.includes("Submitted")?"Application Submitted":key.includes("InterviewCompleted")||key.includes("InterviewAt")?"Interview Completed":key.includes("Interview")?"Interview Scheduled":key.includes("Offer")?"Offer Received":key.includes("counteroffer")?"Counteroffer Made":key.includes("rejected")?"Application Rejected":key.includes("withdrawn")?"Application Withdrawn":key.includes("archived")?"Archived":key.includes("Prepared")?"Application Prepared":key.includes("Response")?"Recruiter Responded":"Updated";
const lifecycleEntries=applications.flatMap((application)=>lifecycleEvents(application).map((event,index)=>make(event.ledgerEntryId??`lifecycle-${application.id}-${index}`,base.length+index+1,"Application",application.id,lifecycleType(event.key),event.label,event.occurredAt,event.occurredAt)));
export const careerLedgerEntries:CareerLedgerEntry[]=[...base,...lifecycleEntries].map((entry,index)=>({...entry,sequenceNumber:index+1}));
export const ledgerForEntity=(id:string)=>careerLedgerEntries.filter((entry)=>entry.entityId===id||entry.relatedEntityIds.includes(id));
