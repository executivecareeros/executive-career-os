import{PageHeader}from"@/components/page-header";import{MemoryCard}from"@/components/memory/memory-card";import{StatCard}from"@/components/stat-card";import{SectionCard}from"@/components/section-card";import{demoAtlasMemories,demoExecutiveIntelligence}from"@/data/atlas-memory";export default function MemoryPage(){return <main className="mx-auto max-w-7xl px-5 py-8 sm:px-6 lg:px-10"><PageHeader eyebrow="Deterministic · Evidence based" title="Executive Memory" description="Career Ledger records what happened. Atlas Memory records transparent observations about what confirmed history may mean."/><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><StatCard label="Confirmed memories" value={demoAtlasMemories.filter(m=>m.status==="Confirmed").length}/><StatCard label="Active patterns" value={demoAtlasMemories.filter(m=>m.status==="Active").length}/><StatCard label="Unknown areas" value="3"/><StatCard label="Recent learnings" value={demoAtlasMemories.length}/><StatCard label="Atlas confidence" value={demoAtlasMemories[0]?.confidence??"Low"}/></div><div className="mt-6 space-y-4">{demoAtlasMemories.length?demoAtlasMemories.map(memory=><MemoryCard key={memory.id} memory={memory}/>):<SectionCard><h2 className="font-semibold">No memories recorded</h2><p className="mt-2 text-sm text-slate-400">Atlas creates observations only after sufficient confirmed evidence exists.</p></SectionCard>}</div><SectionCard className="mt-6"><h2 className="text-xl font-semibold">Executive Intelligence Summary</h2><p className="mt-2 text-sm text-slate-400">Observed counts only. No prediction or personality assessment.</p><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="Industry diversity" value={demoExecutiveIntelligence.industryDiversity}/><StatCard label="Geographic diversity" value={demoExecutiveIntelligence.geographicDiversity}/><StatCard label="Board exposure" value={demoExecutiveIntelligence.boardExposure}/><StatCard label="Transformation records" value={demoExecutiveIntelligence.transformationExperience}/></div></SectionCard><p className="mt-6 text-xs text-slate-600">Fictional demonstration evidence only. No AI, LLM, prediction, chat, or external provider is connected.</p></main>}
