import Link from "next/link";import {AuthField,AuthFrame,FormMessage,SubmitButton} from "@/components/auth/auth-frame";import {forgotPasswordAction} from "../auth-actions";
export default async function Forgot({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <AuthFrame eyebrow="Account recovery" title="Reset your password" description="If the address belongs to an account, we will send secure recovery instructions." footer={<Link className="text-blue-300" href="/login">Return to sign in</Link>}><FormMessage tone="success" message={q.sent?"If an account exists, recovery instructions are on their way.":undefined}/><form action={forgotPasswordAction} className="space-y-5"><AuthField label="Email address" name="email" type="email" autoComplete="email"/><SubmitButton>Send recovery instructions</SubmitButton></form></AuthFrame>}
