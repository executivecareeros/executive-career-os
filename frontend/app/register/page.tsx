import Link from "next/link";import {AuthField,AuthFrame,FormMessage,SubmitButton} from "@/components/auth/auth-frame";import {registerAction} from "../auth-actions";
export default async function Register({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <AuthFrame eyebrow="A permanent professional home" title="Begin your Career Memory" description="Your authentication account attaches securely to a stable Executive Identity. You remain the owner of your career and its history." footer={<>Already have an account? <Link className="text-blue-300" href="/login">Sign in</Link></>}><FormMessage message={q.error}/><form action={registerAction} className="space-y-5"><AuthField label="Email address" name="email" type="email" autoComplete="email"/><AuthField label="Password" name="password" type="password" autoComplete="new-password"/><p className="text-xs text-slate-500">Use at least 8 characters. You will verify your email before continuing.</p><SubmitButton>Create secure account</SubmitButton></form></AuthFrame>}
