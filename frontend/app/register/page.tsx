import Link from "next/link";import {AuthField,AuthFrame,FormMessage,SubmitButton} from "@/components/auth/auth-frame";import {registerAction} from "../auth-actions";
export default async function Register({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;const invite=q.invite?.trim();const invitedEmail=q.email?.trim().toLowerCase();return <AuthFrame eyebrow="Invitation-only private beta" title={invite?"Accept your invitation":"Founder invitation required"} description={invite?"Create the account matching your invitation. The invitation is single-use and expires automatically.":"Public registration is disabled during private beta. Ask the founder for a valid invitation link."} footer={<>Already have an account? <Link className="text-blue-300" href="/login">Sign in</Link></>}>{invite?<><FormMessage message={q.error}/><form action={registerAction} className="space-y-5"><input type="hidden" name="inviteToken" value={invite}/><AuthField label="Invited email address" name="email" type="email" autoComplete="email" defaultValue={invitedEmail} readOnly={Boolean(invitedEmail)}/><AuthField label="Password" name="password" type="password" autoComplete="new-password"/><p className="text-xs text-slate-500">Use at least 8 characters. You will verify your email before continuing.</p><SubmitButton>Create invited account</SubmitButton></form></>:<p role="status" className="rounded-xl border border-amber-400/20 bg-amber-400/10 px-4 py-3 text-sm text-amber-200">Registration cannot continue without a founder-issued invitation.</p>}</AuthFrame>}
