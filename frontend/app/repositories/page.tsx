import {DemoDataBanner} from "@/components/opportunities/demo-data-banner";import {PageHeader} from "@/components/page-header";import {SectionCard} from "@/components/section-card";import {StatusBadge} from "@/components/status-badge";import {repositoryDiagnostics} from "@/lib/repositories";
export default function RepositoriesPage(){return <div className="mx-auto max-w-7xl px-5 py-8 sm:px-6 lg:px-10"><PageHeader eyebrow="Persistence architecture" title="Repository Diagnostics" description="Provider-independent repository registration, health, counts, and capabilities for the deterministic memory runtime."/><div className="mt-6"><DemoDataBanner/></div><SectionCard className="mt-6"><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><caption className="sr-only">Registered repository diagnostics</caption><thead className="border-b border-white/10 text-xs uppercase tracking-wider text-slate-500"><tr>{["Repository","Health","Provider","Records","Version","Read / Write","Append","Transactions"].map(h=><th key={h} scope="col" className="px-3 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-white/5">{repositoryDiagnostics.map(r=><tr key={r.key}><th scope="row" className="px-3 py-4 font-medium text-white">{r.name}</th><td className="px-3 py-4"><StatusBadge tone={r.health.status==="healthy"?"success":"warning"}>{r.health.status}</StatusBadge></td><td className="px-3 py-4 text-slate-400">{r.provider}</td><td className="px-3 py-4">{r.count}</td><td className="px-3 py-4">{r.version}</td><td className="px-3 py-4">{r.capabilities.read?"Read":"—"} / {r.capabilities.write?"Write":"—"}</td><td className="px-3 py-4">{r.appendOnly||r.capabilities.append?"Supported":"—"}</td><td className="px-3 py-4">{r.capabilities.transactions?"Supported":"Future"}</td></tr>)}</tbody></table></div></SectionCard><p className="mt-6 text-xs text-slate-600">Diagnostics only. Records are fictional and reset with the process; no persistence provider is connected.</p></div>}
