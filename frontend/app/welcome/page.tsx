import Link from "next/link";import {currentSession} from "@/lib/auth/session";
export default async function Welcome({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const [session,q]=await Promise.all([currentSession().catch(()=>undefined),searchParams]);const ready=q.ready==="1";return <main className="min-h-screen bg-slate-950 px-6 py-14 text-white"><div className="mx-auto max-w-4xl"><p className="text-xs font-semibold uppercase tracking-[.2em] text-blue-300">Orendalis</p><h1 className="mt-4 text-4xl font-semibold tracking-tight">{ready?"Your Workspace Is Ready":"Welcome."}</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-300">{ready?"Let’s begin with the professional history Atlas needs to help shape your Executive Blueprint.":"Today your Career Ledger begins."}</p><p className="mt-3 max-w-2xl leading-7 text-slate-400">{ready?"One guided journey will take you from confirmed experience to your first evidence-led opportunity decision.":"From this point forward Atlas can help you preserve, understand and improve your professional journey."}</p>{ready?<><div className="mt-12 grid gap-4 sm:grid-cols-2"><Status title="Workspace" text="Your private professional home"/><Status title="Career Ledger" text="Ready to preserve history"/><Status title="Executive Blueprint" text="Ready for progressive completion"/><Status title="Atlas" text="Context ready"/></div><Link href="/beta-workflow#professional-history" className="mt-8 inline-flex min-h-11 items-center rounded-xl bg-white px-6 py-3 text-sm font-semibold text-slate-950 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-400">Continue</Link><p className="mt-3 text-sm text-slate-500">Next: confirm one role or review a CV/resume draft.</p></>:<div className="mt-10 rounded-2xl border border-white/10 bg-white/[.03] p-6"><p className="font-medium">Your secure session is ready{session?.user.email?` for ${session.user.email}`:""}.</p><p className="mt-2 text-sm text-slate-400">Create your personal workspace and begin with only the context Atlas needs.</p><Link href="/onboarding" className="mt-5 inline-flex rounded-xl bg-white px-5 py-3 text-sm font-semibold text-slate-950">Continue to onboarding</Link></div>}</div></main>}
function Status({title,text}:{title:string;text:string}){return <section className="rounded-2xl border border-white/10 bg-slate-900 p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">{title}</h2><span className="rounded-full bg-emerald-400/10 px-2.5 py-1 text-xs text-emerald-300">Ready</span></div><p className="mt-2 text-sm text-slate-400">{text}</p></section>}
