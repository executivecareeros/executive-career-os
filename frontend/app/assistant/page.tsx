import { AgentHealthCard } from "@/components/agents/agent-health-card";import { DemoDataBanner } from "@/components/opportunities/demo-data-banner";import { PageHeader } from "@/components/page-header";import { SectionCard } from "@/components/section-card";import { StatCard } from "@/components/stat-card";import { StatusBadge } from "@/components/status-badge";import { agentHealth,agentTasks,mockRecommendations } from "@/data/agent-dashboard";
export default function AssistantPage(){const pending=agentTasks.filter(t=>t.state==="pending");const completed=agentTasks.filter(t=>t.state==="completed");return <div className="mx-auto max-w-7xl px-5 py-8 sm:px-6 lg:px-10"><PageHeader title="Executive Career Agent" description="Architecture observability for future autonomous career intelligence. No AI provider or automation is connected."/><div className="mt-6"><DemoDataBanner/></div><section className="grid gap-4 py-6 sm:grid-cols-2 xl:grid-cols-4" aria-label="Agent metrics"><StatCard label="Healthy agents" value={agentHealth.filter(a=>a.status!=="failed").length} note="Mock health state"/><StatCard label="Pending tasks" value={pending.length}/><StatCard label="Completed tasks" value={completed.length}/><StatCard label="Memory usage" value={`${agentHealth.reduce((n,a)=>n+a.memoryEntries,0)} entries`}/></section><SectionCard><h2 className="text-xl font-semibold">Agent health</h2><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-5">{agentHealth.map(a=><AgentHealthCard key={a.agent} record={a}/>)}</div></SectionCard><div className="mt-6 grid gap-6 xl:grid-cols-2"><SectionCard><h2 className="text-xl font-semibold">Task queue and upcoming schedule</h2><div className="mt-4 space-y-3">{agentTasks.map(t=><article key={t.id} className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><div className="flex justify-between gap-3"><div><p className="font-medium">{t.title}</p><p className="mt-1 text-sm text-slate-500">{t.agent} · {t.scheduledFor??"Completed"}</p></div><StatusBadge tone={t.state==="completed"?"success":"info"}>{t.state}</StatusBadge></div></article>)}</div></SectionCard><SectionCard><h2 className="text-xl font-semibold">Current recommendations</h2><div className="mt-4 space-y-3">{mockRecommendations.map(r=><article key={r.id} className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><p className="font-medium">{r.title}</p><p className="mt-1 text-sm text-slate-500">{r.agent} · {r.priority} priority · mock recommendation</p></article>)}</div></SectionCard></div><p className="mt-6 text-xs text-slate-600">Framework preview only. Schedules do not execute, notifications are not delivered, and prompts are never sent.</p></div>}
