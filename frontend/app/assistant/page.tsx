import { AgentHealthCard } from "@/components/agents/agent-health-card";import { DemoDataBanner } from "@/components/opportunities/demo-data-banner";import { PageHeader } from "@/components/page-header";import { SecondaryButton } from "@/components/secondary-button";import { SectionCard } from "@/components/section-card";import { StatCard } from "@/components/stat-card";import { StatusBadge } from "@/components/status-badge";import { agentHealth,agentTasks,mockRecommendations } from "@/data/agent-dashboard";
export default function AssistantPage(){const pending=agentTasks.filter(task=>task.state==="pending");const completed=agentTasks.filter(task=>task.state==="completed");return <div className="mx-auto max-w-7xl px-5 py-8 sm:px-6 lg:px-10"><PageHeader title="Executive Career Agent" description="Architecture observability for future autonomous career intelligence. No AI provider or automation is connected." actions={<><SecondaryButton href="/archive">Open agent history</SecondaryButton><SecondaryButton href="/compensation">Compensation history</SecondaryButton></>}/><div className="mt-6"><DemoDataBanner/></div><section className="grid gap-4 py-6 sm:grid-cols-2 xl:grid-cols-4" aria-label="Agent metrics"><StatCard label="Healthy agents" value={agentHealth.filter(agent=>agent.status!=="failed").length} note="Mock health state"/><StatCard label="Pending tasks" value={pending.length}/><StatCard label="Completed tasks" value={completed.length}/><StatCard label="Memory usage" value={`${agentHealth.reduce((count,agent)=>count+agent.memoryEntries,0)} entries`}/></section><SectionCard><h2 className="text-xl font-semibold">Agent health</h2><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-5">{agentHealth.map(agent=><AgentHealthCard key={agent.agent} record={agent}/>)}</div></SectionCard><div className="mt-6 grid gap-6 xl:grid-cols-2"><SectionCard><h2 className="text-xl font-semibold">Task queue and upcoming schedule</h2><div className="mt-4 space-y-3">{agentTasks.map(task=><article key={task.id} className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><div className="flex justify-between gap-3"><div><p className="font-medium">{task.title}</p><p className="mt-1 text-sm text-slate-500">{task.agent} · {task.scheduledFor??"Completed"}</p></div><StatusBadge tone={task.state==="completed"?"success":"info"}>{task.state}</StatusBadge></div></article>)}</div></SectionCard><SectionCard><h2 className="text-xl font-semibold">Current recommendations</h2><div className="mt-4 space-y-3">{mockRecommendations.map(recommendation=><article key={recommendation.id} className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><p className="font-medium">{recommendation.title}</p><p className="mt-1 text-sm text-slate-500">{recommendation.agent} · {recommendation.priority} priority · mock recommendation</p></article>)}</div></SectionCard></div><p className="mt-6 text-xs text-slate-600">Framework preview only. Schedules do not execute, notifications are not delivered, and prompts are never sent.</p></div>}
