import Link from "next/link";
import { ArchiveWorkspace } from "@/components/archive/archive-workspace";import {LiveWorkspaceEmptyState} from "@/components/live-workspace-empty-state";import { careerLedgerEntries } from "@/data/career-ledger";
import { resolveAuthenticatedRepositoryContext } from "@/lib/auth/repository-context";import { createServerSupabaseClient } from "@/lib/supabase/server";import { loadLatestCollectedDecision } from "@/lib/live-collected-decision";
export default async function ArchivePage(){if(process.env.NEXT_PUBLIC_DATA_ACCESS_MODE==="supabase"){const resolved=await resolveAuthenticatedRepositoryContext();const decision=resolved?await loadLatestCollectedDecision(createServerSupabaseClient(resolved.accessToken),resolved.context.workspace!.workspaceId):undefined;if(!decision)return <LiveWorkspaceEmptyState title="Career Ledger" description="Your permanent record of confirmed career events and decisions." emptyTitle="No executive decisions recorded yet" emptyDescription="Your first finalized opportunity decision will appear here with its evidence and context." actionHref="/opportunities" actionLabel="Explore opportunities"/>;return <main className="mx-auto max-w-5xl px-5 py-8 sm:px-6 lg:px-10"><p className="text-sm uppercase tracking-[.18em] text-blue-300">Career Ledger</p><h1 className="mt-3 text-3xl font-semibold">A permanent record of your decisions</h1><article className="mt-7 rounded-2xl border border-white/10 bg-white/[.04] p-6"><p className="text-sm text-emerald-300">Finalized · {decision.action}</p><h2 className="mt-2 text-xl font-semibold">{decision.title}</h2><p className="mt-1 text-slate-400">{decision.companyName}</p><p className="mt-4 text-sm text-slate-300">The executive decision, Atlas evidence snapshot, and follow-up were preserved together.</p><Link href={`/opportunities/${encodeURIComponent(decision.opportunityId)}`} className="mt-5 inline-flex text-sm font-medium text-blue-300">Review decision evidence →</Link></article></main>}return <ArchiveWorkspace entries={careerLedgerEntries}/>}
