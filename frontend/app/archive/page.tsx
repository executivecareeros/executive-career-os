import { ArchiveWorkspace } from "@/components/archive/archive-workspace";import {LiveWorkspaceEmptyState} from "@/components/live-workspace-empty-state";import { careerLedgerEntries } from "@/data/career-ledger";export default function ArchivePage(){if(process.env.NEXT_PUBLIC_DATA_ACCESS_MODE==="supabase")return <LiveWorkspaceEmptyState title="Career Ledger" description="Your permanent record of confirmed career events and decisions." emptyTitle="No executive decisions recorded yet" emptyDescription="Your first finalized opportunity decision will appear here with its evidence and context." actionHref="/beta-workflow" actionLabel="Continue your first assessment"/>;return <ArchiveWorkspace entries={careerLedgerEntries}/>}
