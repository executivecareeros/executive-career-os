import { BlueprintWorkspace } from "@/components/blueprint/blueprint-workspace";import {LiveWorkspaceEmptyState} from "@/components/live-workspace-empty-state";import { demoExecutiveBlueprint } from "@/data/executive-blueprint";export default function BlueprintPage(){if(process.env.NEXT_PUBLIC_DATA_ACCESS_MODE==="supabase")return <LiveWorkspaceEmptyState title="Executive Blueprint" description="Define the direction, preferences, and constraints Atlas must respect." emptyTitle="Your Blueprint has not been confirmed yet" emptyDescription="Start with one professional-history record. The guided journey will then open the minimum Blueprint questions without inventing answers." actionHref="/beta-workflow#professional-history" actionLabel="Confirm professional history"/>;return <BlueprintWorkspace initial={demoExecutiveBlueprint}/>}
