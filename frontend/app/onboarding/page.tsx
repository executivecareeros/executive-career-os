import {redirect} from "next/navigation";import {AuthField,AuthFrame,FormMessage,SubmitButton} from "@/components/auth/auth-frame";import {currentSession} from "@/lib/auth/session";import {onboardingAction} from "../auth-actions";
export default async function Onboarding({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const [q,session]=await Promise.all([searchParams,currentSession()]);if(!session)redirect("/login?next=/onboarding");return <AuthFrame eyebrow="Progressive memory · Step 1 of 1" title="Create your professional home" description="Only the context needed to begin. Your Executive Blueprint will develop separately, at your pace."><FormMessage message={q.error}/><form action={onboardingAction} className="space-y-5"><AuthField label="Preferred name" name="preferredName" autoComplete="given-name"/><AuthField label="Current role" name="currentRole"/><AuthField label="Current employer (optional)" name="currentEmployer" required={false}/><AuthField label="Country" name="country" autoComplete="country-name"/><div className="grid gap-5 sm:grid-cols-2"><AuthField label="Preferred language" name="preferredLanguage" placeholder="English"/><AuthField label="Timezone" name="timezone" placeholder="Europe/Istanbul"/></div><label className="block text-sm font-medium text-slate-200">Long-term career ambition<textarea name="careerAmbition" required rows={4} className="mt-2 w-full resize-none rounded-xl border border-white/10 bg-slate-950 px-4 py-3 outline-none focus:border-blue-400" placeholder="The direction you want your career to take."/></label><section className="rounded-2xl border border-blue-400/20 bg-blue-400/5 p-5"><p className="text-xs font-semibold uppercase tracking-[.18em] text-blue-300">The Atlas Promise</p><h2 className="mt-2 text-lg font-semibold">Trust before intelligence</h2><ul className="mt-3 space-y-2 text-sm leading-6 text-slate-300"><li>You own your career.</li><li>Atlas explains every recommendation.</li><li>Atlas never silently changes your history.</li><li>Your data is portable through Career Passport.</li><li>You control who can access your information.</li></ul><label className="mt-5 flex items-start gap-3 text-sm text-white"><input required name="atlasPromiseAccepted" type="checkbox" className="mt-1 accent-blue-500"/><span>I understand and accept the Atlas Promise.</span></label></section><SubmitButton>Create my Career Memory</SubmitButton></form></AuthFrame>}
