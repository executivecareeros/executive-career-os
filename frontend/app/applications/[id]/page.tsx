import { notFound } from "next/navigation";import { ApplicationDetail } from "@/components/applications/application-detail";import { applications,getApplicationById } from "@/data/applications";import { getOpportunityById } from "@/data/opportunities";import { getCompanyById } from "@/data/companies";export const generateStaticParams=()=>applications.map(a=>({id:a.id}));export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const a=getApplicationById(id);if(!a)notFound();const o=getOpportunityById(a.opportunityId),c=getCompanyById(a.companyId);if(!o||!c)notFound();return <ApplicationDetail application={a} opportunity={o} company={c}/>}
