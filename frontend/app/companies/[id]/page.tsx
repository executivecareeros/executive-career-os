import { notFound } from "next/navigation";import { CompanyDetail } from "@/components/companies/company-detail";import { CompanyHistoryIntegration } from "@/components/companies/company-history-integration";import { CompanyBlueprintAlignment } from "@/components/blueprint/company-alignment";import { companies,getCompanyById } from "@/data/companies";import { opportunities } from "@/data/opportunities";
export function generateStaticParams(){return companies.map((company)=>({id:company.id}));}
export default async function CompanyPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const company=getCompanyById(id);if(!company)notFound();const linked=opportunities.filter((opportunity)=>company.opportunityIds.includes(opportunity.id));return <><CompanyDetail company={company} linked={linked}/><div className="mx-auto max-w-7xl px-5 pb-6 sm:px-6 lg:px-10"><CompanyBlueprintAlignment company={company}/></div><CompanyHistoryIntegration companyId={company.id}/></>}
