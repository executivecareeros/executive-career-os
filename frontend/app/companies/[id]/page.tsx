import { notFound } from "next/navigation";import { CompanyDetail } from "@/components/companies/company-detail";import { CompanyHistoryIntegration } from "@/components/companies/company-history-integration";import { companies,getCompanyById } from "@/data/companies";import { opportunities } from "@/data/opportunities";
export function generateStaticParams(){return companies.map((company)=>({id:company.id}));}
export default async function CompanyPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const company=getCompanyById(id);if(!company)notFound();const linked=opportunities.filter((opportunity)=>company.opportunityIds.includes(opportunity.id));return <><CompanyDetail company={company} linked={linked}/><CompanyHistoryIntegration companyId={company.id}/></>}
