"use client";
import {useEffect,useState} from "react";import {AuthField,AuthFrame,FormMessage,SubmitButton} from "@/components/auth/auth-frame";import {resetPasswordAction} from "../auth-actions";
export default function Reset(){const[ready,setReady]=useState(false);const[error,setError]=useState<string>();useEffect(()=>{const values=new URLSearchParams(location.hash.slice(1));const access_token=values.get("access_token"),refresh_token=values.get("refresh_token"),expires_in=Number(values.get("expires_in")??3600);if(!access_token||!refresh_token){queueMicrotask(()=>setError("This recovery link is incomplete or has expired."));return;}fetch("/api/auth/recovery-session",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({access_token,refresh_token,expires_in})}).then((response)=>{if(!response.ok)throw new Error();history.replaceState(null,"",location.pathname);setReady(true)}).catch(()=>setError("The recovery session could not be established."));},[]);return <AuthFrame eyebrow="Secure recovery" title="Choose a new password" description="Your recovery session is used once to update your credentials."><FormMessage message={error}/>{ready?<form action={resetPasswordAction} className="space-y-5"><AuthField label="New password" name="password" type="password" autoComplete="new-password"/><SubmitButton>Update password</SubmitButton></form>:!error?<p className="text-sm text-slate-400">Validating your recovery session…</p>:null}</AuthFrame>}
