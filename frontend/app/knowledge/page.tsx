import { DemoDataBanner } from "@/components/opportunities/demo-data-banner";import { MetricCard } from "@/components/metric-card";import { PageHeader } from "@/components/page-header";import { SectionCard } from "@/components/section-card";import { StatusBadge } from "@/components/status-badge";import { demoKnowledgeDigest,knowledgeAssessments,knowledgeSignals,knowledgeWatchlists } from "@/data/knowledge-network";
export default function KnowledgePage(){const categories=["Companies","Industries","Countries","Compensation","Executive Search","Leadership","Technology"] as const;return <div className="mx-auto max-w-7xl px-5 py-8 sm:px-6 lg:px-10"><PageHeader eyebrow="Executive Market" title="Atlas Knowledge Network" description="Deterministic observations about the executive market, connected through evidence and typed relationships."/><div className="mt-6"><DemoDataBanner/></div><section className="grid gap-4 py-6 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Recent signals" value={knowledgeSignals.length}/><MetricCard label="Watchlists" value={knowledgeWatchlists.length}/><MetricCard label="Verified signals" value={knowledgeSignals.filter(s=>s.lifecycle==="Verified").length}/><MetricCard label="Average confidence" value={`${Math.round(knowledgeAssessments.reduce((n,a)=>n+a.confidenceScore,0)/knowledgeAssessments.length)}%`}/></section><SectionCard><h2 className="text-xl font-semibold">Recent Signals</h2><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{knowledgeSignals.map(s=>{const a=knowledgeAssessments.find(x=>x.signalId===s.id);return <article key={s.id} className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><div className="flex justify-between gap-3"><p className="text-xs uppercase tracking-wider text-slate-500">{s.category}</p><StatusBadge tone={s.lifecycle==="Verified"?"success":"warning"}>{s.lifecycle}</StatusBadge></div><h3 className="mt-3 font-medium">{s.title}</h3><p className="mt-2 text-xs text-slate-500">{s.observedAt} · {a?.confidence} confidence ({a?.confidenceScore}%)</p></article>})}</div></SectionCard><div className="mt-6 grid gap-6 xl:grid-cols-2"><SectionCard><h2 className="text-xl font-semibold">Watchlists</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{knowledgeWatchlists.map(w=><article key={w.id} className="rounded-xl border border-white/10 p-4"><p className="font-medium">{w.name}</p><p className="mt-1 text-xs text-slate-500">{w.topicIds.length} demonstration topics · future integrations only</p></article>)}</div></SectionCard><SectionCard><h2 className="text-xl font-semibold">Digest Preview</h2><p className="mt-2 text-sm text-slate-400">Structured brief preparation; no generated text.</p><div className="mt-4 space-y-3"><Digest title="What changed?" values={demoKnowledgeDigest.whatChanged}/><Digest title="Why it matters" values={demoKnowledgeDigest.whyItMatters}/><Digest title="Recommended review" values={demoKnowledgeDigest.recommendedReviewCodes}/></div></SectionCard></div><SectionCard className="mt-6"><h2 className="text-xl font-semibold">Knowledge Domains</h2><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{categories.map(c=><article key={c} className="rounded-xl border border-white/10 p-4"><p className="font-medium">{c}</p><p className="mt-2 text-xs text-slate-500">Timeline and Atlas observations available as demonstration architecture.</p></article>)}</div></SectionCard><SectionCard className="mt-6"><h2 className="text-xl font-semibold">Knowledge Timeline</h2><ol className="mt-4 border-l border-white/10 pl-5">{[...knowledgeSignals].sort((a,b)=>b.observedAt.localeCompare(a.observedAt)).map(s=><li key={s.id} className="relative pb-5 before:absolute before:-left-[1.55rem] before:top-1 before:h-2 before:w-2 before:rounded-full before:bg-blue-400"><p className="text-sm font-medium">{s.code}</p><p className="mt-1 text-xs text-slate-500">{s.observedAt} · {s.lifecycle}</p></li>)}</ol></SectionCard><p className="mt-6 text-xs text-slate-600">All signals, providers, observations, and relationships are fictional demonstration data. No external source is connected.</p></div>}
function Digest({title,values}:{title:string;values:readonly string[]}){return <div className="rounded-xl border border-white/10 p-4"><h3 className="font-medium">{title}</h3><ul className="mt-2 space-y-1 text-xs text-slate-500">{values.map(v=><li key={v}>{v}</li>)}</ul></div>}
