import { SectionCard } from "@/components/section-card";import { StatusBadge } from "@/components/status-badge";import { knowledgeAssessments,signalsForEntity } from "@/data/knowledge-network";
export function KnowledgePanel({entityId,title="Relevant Knowledge Signals"}:{entityId:string;title?:string}){const signals=signalsForEntity(entityId);return <SectionCard><div className="flex flex-wrap justify-between gap-3"><div><h2 className="text-xl font-semibold">{title}</h2><p className="mt-2 text-sm text-slate-400">Observed changes, timeline, and confidence from fictional demonstration evidence.</p></div><StatusBadge tone="info">Demo only</StatusBadge></div><div className="mt-5 grid gap-4 md:grid-cols-2">{signals.map(s=>{const a=knowledgeAssessments.find(x=>x.signalId===s.id);return <article key={s.id} className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><div className="flex justify-between gap-3"><div><p className="text-xs uppercase tracking-wider text-slate-500">{s.category}</p><h3 className="mt-1 font-medium">{s.title}</h3></div><StatusBadge tone={s.lifecycle==="Verified"?"success":"warning"}>{s.lifecycle}</StatusBadge></div><p className="mt-3 text-sm text-slate-400">{s.code}</p><p className="mt-3 text-xs text-slate-500">Knowledge confidence: {a?.confidence??"Very Low"} · {a?.confidenceScore??0}% · observed {s.observedAt}</p></article>})}{!signals.length&&<p className="text-sm text-slate-500">No related demonstration signals.</p>}</div></SectionCard>}
