import { StatusBadge } from "@/components/status-badge";import type { AgentHealthRecord } from "@/data/agent-dashboard";
export function AgentHealthCard({record}:{record:AgentHealthRecord}){return <article className="rounded-xl border border-white/10 bg-slate-950/40 p-5"><div className="flex items-start justify-between gap-3"><h3 className="font-semibold">{record.label}</h3><StatusBadge tone={record.status==="failed"?"warning":record.status==="scheduled"?"info":record.status==="idle"?"success":"neutral"}>{record.status}</StatusBadge></div><dl className="mt-5 space-y-3 text-sm"><Row label="Last run" value={record.lastRun??"Never"}/><Row label="Next run" value={record.nextRun??"Not scheduled"}/><Row label="Memory" value={`${record.memoryEntries} entries`}/></dl></article>}
function Row({label,value}:{label:string;value:string}){return <div className="flex justify-between gap-3"><dt className="text-slate-500">{label}</dt><dd className="text-right text-slate-300">{value}</dd></div>}
