import { SectionCard } from "@/components/section-card";import { PremiumIndicator } from "./premium-indicator";import type { FeatureAccessResult } from "@/types/entitlements";export function UpgradePanel({result}:{result:FeatureAccessResult}){return <SectionCard><div className="flex flex-wrap items-center justify-between gap-3"><h3 className="font-semibold text-white">{result.feature.title}</h3><PremiumIndicator plan={result.feature.minimumPlan} status={result.feature.status}/></div><p className="mt-3 text-sm text-slate-400">{result.feature.description}</p><p className="mt-3 text-sm text-amber-200">{result.explanation}</p><p className="mt-3 text-xs text-slate-600">Entitlement preview only. No billing or checkout is active.</p></SectionCard>}
