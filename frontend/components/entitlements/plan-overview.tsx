import { SectionCard } from "@/components/section-card";import { StatusBadge } from "@/components/status-badge";import { UpgradePanel } from "./upgrade-panel";import { demoEntitlementContext } from "@/data/entitlements";import { featureRegistry } from "@/lib/feature-registry";import { canAccessFeature,getRemainingUsage } from "@/lib/entitlement-service";
export function PlanOverview({compact=false}:{compact?:boolean}){const preview=["blueprint-recommendations","autonomous-monitoring"].map(k=>canAccessFeature(k,demoEntitlementContext));return <div className="space-y-6"><SectionCard><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-semibold">Current demonstration plan</h2><p className="mt-2 text-sm text-slate-400">The free product remains useful and preserves access to the core career record.</p></div><StatusBadge tone="success">Atlas Free</StatusBadge></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{featureRegistry.filter(f=>f.minimumPlan==="free").map(f=><article key={f.featureKey} className="rounded-xl border border-white/10 p-4"><p className="font-medium">{f.title}</p><p className="mt-1 text-xs text-slate-500">{f.status}{f.featureKey==="atlas-basic"?` · ${getRemainingUsage(f.featureKey,demoEntitlementContext)} uses remaining`:""}</p></article>)}</div><p className="mt-4 text-sm text-amber-200">No billing is active. There are no subscription or checkout controls.</p></SectionCard>{!compact&&<div className="grid gap-6 lg:grid-cols-2">{preview.map(r=><UpgradePanel key={r.feature.featureKey} result={r}/>)}</div>}</div>}
