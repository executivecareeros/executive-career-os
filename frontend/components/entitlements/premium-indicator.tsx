import { StatusBadge } from "@/components/status-badge";import type { FeatureStatus,ProductPlan } from "@/types/entitlements";export function PremiumIndicator({plan,status}:{plan:ProductPlan;status:FeatureStatus}){return <StatusBadge tone={status==="Available"?"success":status==="Preview"?"info":"warning"}>{plan==="free"?"Free":plan==="atlas-pro"?"Atlas Pro":"Atlas Executive"} · {status}</StatusBadge>}
