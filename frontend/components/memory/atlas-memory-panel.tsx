import{demoAtlasMemories}from"@/data/atlas-memory";import{SectionCard}from"@/components/section-card";export function AtlasMemoryPanel(){const confirmed=demoAtlasMemories.filter(m=>m.status==="Confirmed"),suspected=demoAtlasMemories.filter(m=>m.status!=="Confirmed");return <SectionCard><h2 className="text-xl font-semibold">Atlas Memory</h2><p className="mt-2 text-sm text-slate-400">Transparent observations derived only from confirmed fictional demonstration records.</p><div className="mt-5 grid gap-4 md:grid-cols-2"><Group title="What Atlas knows" items={confirmed.map(m=>m.statement)} empty="No fully confirmed memories yet."/><Group title="What Atlas suspects" items={suspected.map(m=>m.statement)} empty="No active hypotheses."/><Group title="What Atlas still doesn&apos;t know" items={["Directly confirmed travel preferences","Compensation principles","Preferred company ownership"]}/><Group title="What Atlas needs confirmed" items={demoAtlasMemories.flatMap(m=>m.missingEvidence)} empty="Nothing awaiting confirmation."/></div></SectionCard>}function Group({title,items,empty}:{title:string;items:readonly string[];empty?:string}){return <section className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><h3 className="font-medium">{title}</h3><ul className="mt-3 space-y-2 text-sm text-slate-400">{items.length?items.map(item=><li key={item}>• {item}</li>):<li>{empty}</li>}</ul></section>}
