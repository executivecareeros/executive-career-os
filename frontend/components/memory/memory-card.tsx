import type{MemoryObservation}from"@/lib/memory";export function MemoryCard({memory}:{memory:MemoryObservation}){return <article className="rounded-2xl border border-white/10 bg-white/[.02] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-blue-300">{memory.category}</p><h2 className="mt-2 text-lg font-semibold">{memory.statement}</h2></div><div className="flex gap-2"><span className="rounded-full border border-white/10 px-3 py-1 text-xs">{memory.confidence}</span><span className="rounded-full bg-emerald-400/10 px-3 py-1 text-xs text-emerald-300">{memory.status}</span></div></div><details className="mt-5 rounded-xl border border-white/10 p-4"><summary className="cursor-pointer text-sm font-medium">Why Atlas recorded this</summary><div className="mt-3 space-y-3 text-sm text-slate-400"><p>Reason codes: {memory.reasonCodes.join(" · ")}</p><div><strong className="text-slate-200">Supporting evidence</strong>{memory.evidence.map(item=><p key={item.id} className="mt-1">{item.source} · {item.sourceRecordId} · {item.observedAt}</p>)}</div><div><strong className="text-slate-200">Contradicting evidence</strong><p className="mt-1">{memory.contradictingEvidence.length?memory.contradictingEvidence.map(e=>e.sourceRecordId).join(", "):"None recorded"}</p></div><div><strong className="text-slate-200">Missing evidence</strong><p className="mt-1">{memory.missingEvidence.length?memory.missingEvidence.join(" · "):"No missing evidence identified"}</p></div></div></details></article>}
