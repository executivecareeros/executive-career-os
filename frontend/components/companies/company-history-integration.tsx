import Link from "next/link";import { SectionCard } from "@/components/section-card";import { CompensationTimeline } from "@/components/compensation/compensation-timeline";import { LedgerHistoryPanel } from "@/components/archive/ledger-history-panel";import { compensationForEntity } from "@/data/compensation";import { ledgerForEntity } from "@/data/career-ledger";import { getApplicationsByCompany } from "@/data/applications";
export function CompanyHistoryIntegration({companyId}:{companyId:string}){const compensation=compensationForEntity(companyId);const history=ledgerForEntity(companyId);const applications=getApplicationsByCompany(companyId);return <div className="mx-auto max-w-7xl px-5 pb-8 sm:px-6 lg:px-10"><SectionCard><div className="flex justify-between gap-4"><div><h2 className="text-xl font-semibold">Historical intelligence</h2><p className="mt-2 text-sm text-slate-400">{applications.length} linked demo application(s). Statistics are not generalized from this limited sample.</p></div><Link href={`/archive?company=${companyId}`} className="text-sm text-blue-300">Open company history →</Link></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><CompensationTimeline records={compensation} title="Company Compensation History"/><LedgerHistoryPanel entries={history}/></div></SectionCard></div>}
