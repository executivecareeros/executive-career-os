import { StatusBadge } from "@/components/status-badge"; import type { ApplicationOutcome,ApplicationPriority,ApplicationStage } from "@/types/application"; import type { Attention } from "@/lib/application-attention";
export const ApplicationStageBadge=({value}:{value:ApplicationStage})=><StatusBadge tone={["Offer","Interview","Final Interview"].includes(value)?"success":["Rejected","Withdrawn"].includes(value)?"warning":"info"}>{value}</StatusBadge>;export const ApplicationOutcomeBadge=({value}:{value:ApplicationOutcome})=><StatusBadge tone={value==="Active"?"success":value==="Offer Received"?"info":"neutral"}>{value}</StatusBadge>;export const ApplicationPriorityBadge=({value}:{value:ApplicationPriority})=><StatusBadge tone={value==="Critical"||value==="High"?"warning":"neutral"}>{value}</StatusBadge>;export const ApplicationAttentionBadge=({value}:{value:Attention})=><StatusBadge tone={value==="On Track"?"success":value==="Overdue"||value==="Blocked"?"warning":"info"}>{value}</StatusBadge>;
