import type { DiscoveryRun } from "@/lib/discovery";
import { StatusBadge } from "@/components/status-badge";

function duration(milliseconds?: number) { return milliseconds === undefined ? "—" : `${(milliseconds / 1000).toFixed(0)}s`; }
export function RunHistoryTable({ runs }: { runs: readonly DiscoveryRun[] }) {
  return <div className="overflow-x-auto"><table className="w-full min-w-[860px] text-left text-sm"><caption className="sr-only">Demonstration discovery run history</caption><thead className="border-b border-white/10 text-xs uppercase tracking-wider text-slate-500"><tr>{["Run ID", "Source", "Started", "Finished", "Duration", "Found", "Imported", "Ignored", "Errors", "Warnings"].map((heading) => <th key={heading} scope="col" className="px-3 py-3 font-medium">{heading}</th>)}</tr></thead><tbody className="divide-y divide-white/5">{runs.map((run) => <tr key={run.id} className="text-slate-300"><th scope="row" className="px-3 py-4 font-medium text-white">{run.id}<span className="mt-1 block"><StatusBadge tone={run.status === "completed" ? "success" : "warning"}>{run.status}</StatusBadge></span></th><td className="px-3 py-4">{run.source}</td><td className="px-3 py-4">{new Date(run.startedAt).toLocaleString("en-GB", { dateStyle: "medium", timeStyle: "short" })}</td><td className="px-3 py-4">{run.finishedAt ? new Date(run.finishedAt).toLocaleTimeString("en-GB", { hour: "2-digit", minute: "2-digit" }) : "—"}</td><td className="px-3 py-4">{duration(run.durationMs)}</td><td className="px-3 py-4">{run.jobsFound}</td><td className="px-3 py-4">{run.jobsImported}</td><td className="px-3 py-4">{run.jobsIgnored}</td><td className="px-3 py-4">{run.errors.length}</td><td className="px-3 py-4">{run.warnings.length}</td></tr>)}</tbody></table></div>;
}
