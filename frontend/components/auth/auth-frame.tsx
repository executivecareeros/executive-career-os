import Link from "next/link";
import { OrendalisMark } from "@/components/brand/orendalis-mark";
import { LanguageSwitcher } from "@/components/language-switcher";
import type { Locale } from "@/lib/locale";

export function AuthFrame({ eyebrow, title, description, children, footer, locale = "en" }: { eyebrow: string; title: string; description: string; children: React.ReactNode; footer?: React.ReactNode; locale?: Locale }) {
  return <main className="flex min-h-screen items-center justify-center bg-[#f7f3ec] px-6 py-12 text-[#22211f]"><section className="w-full max-w-md"><div className="mb-10 flex items-center justify-between"><Link href="/" className="inline-flex text-[#22211f]"><OrendalisMark /></Link><LanguageSwitcher locale={locale}/></div><div className="rounded-[1.75rem] border border-[#e3ddd3] bg-[#fffdf9] p-7 shadow-[0_24px_70px_rgba(54,43,31,.08)] sm:p-9"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#936b3f]">{eyebrow}</p><h1 className="orendalis-display mt-4 text-4xl leading-tight tracking-[-0.04em]">{title}</h1><p className="mt-4 text-sm leading-6 text-[#6f6a64]">{description}</p><div className="mt-8">{children}</div>{footer && <div className="mt-7 border-t border-[#e8e1d8] pt-6 text-sm text-[#746f69]">{footer}</div>}</div><p className="mt-6 text-center text-xs leading-5 text-[#7c766f]">{locale === "tr" ? "Gizlilik en başından tasarlandı. Karar her zaman senin." : "Private by design. Your judgment remains yours."}</p></section></main>;
}
export function AuthField({label,name,type="text",required=true,autoComplete,placeholder,defaultValue,readOnly=false}:{label:string;name:string;type?:string;required?:boolean;autoComplete?:string;placeholder?:string;defaultValue?:string;readOnly?:boolean}){return <label className="block text-sm font-medium text-[#34312d]">{label}<input name={name} type={type} required={required} autoComplete={autoComplete} placeholder={placeholder} defaultValue={defaultValue} readOnly={readOnly} className="mt-2 w-full rounded-xl border border-[#d8d0c5] bg-white px-4 py-3 text-[#22211f] outline-none transition placeholder:text-[#9b948c] read-only:cursor-not-allowed read-only:text-[#746f69] focus:border-[#936b3f] focus:ring-2 focus:ring-[#936b3f]/15"/></label>}
export function SubmitButton({children}:{children:React.ReactNode}){return <button className="w-full rounded-full bg-[#22211f] px-4 py-3 text-sm font-semibold text-white transition hover:bg-black focus:outline-none focus-visible:ring-2 focus-visible:ring-[#936b3f]">{children}</button>}
export function FormMessage({message,tone="error"}:{message?:string;tone?:"error"|"success"}){return message?<p role="status" className={`mb-5 rounded-xl border px-4 py-3 text-sm ${tone==="error"?"border-rose-200 bg-rose-50 text-rose-800":"border-emerald-200 bg-emerald-50 text-emerald-800"}`}>{message}</p>:null}
