import Link from "next/link";import type { CareerLedgerEntry } from "@/types/career-ledger";import { StatusBadge } from "@/components/status-badge";
export function LedgerEntryCard({entry}:{entry:CareerLedgerEntry}){return <article className="rounded-xl border border-white/10 bg-slate-950/40 p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-xs text-slate-500">#{entry.sequenceNumber} · {entry.entityType}</p><h3 className="mt-1 font-semibold">{entry.title}</h3></div><div className="flex gap-2"><StatusBadge tone="info">{entry.eventType}</StatusBadge>{entry.isArchived&&<StatusBadge tone="warning">Archived</StatusBadge>}</div></div><p className="mt-3 text-sm text-slate-400">{entry.summary}</p><p className="mt-3 text-xs text-slate-500">{new Date(entry.occurredAt).toLocaleString("en-GB")} · {entry.actorType} · Confidence {entry.confidence}/100</p><Link href={`/archive/${entry.id}`} className="mt-4 inline-flex text-sm text-blue-300">Open ledger entry →</Link></article>}
