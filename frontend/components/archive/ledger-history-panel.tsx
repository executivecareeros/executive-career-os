import type { CareerLedgerEntry } from "@/types/career-ledger";import { LedgerEntryCard } from "./ledger-entry-card";
export function LedgerHistoryPanel({entries,title="Career Ledger history"}:{entries:CareerLedgerEntry[];title?:string}){return <section><h2 className="text-lg font-semibold text-white">{title}</h2>{entries.length?<div className="mt-4 space-y-3">{[...entries].sort((a,b)=>b.occurredAt.localeCompare(a.occurredAt)).map((entry)=><LedgerEntryCard key={entry.id} entry={entry}/>)}</div>:<p className="mt-3 text-sm text-slate-500">No fictional ledger history is linked.</p>}</section>}
