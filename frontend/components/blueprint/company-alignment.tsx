import Link from "next/link";import { SectionCard } from "@/components/section-card";import { StatusBadge } from "@/components/status-badge";import { demoExecutiveBlueprint } from "@/data/executive-blueprint";import { matchCompanyToBlueprint } from "@/lib/blueprint-matching";import type { Company } from "@/types/company";
export function CompanyBlueprintAlignment({company}:{company:Company}){const a=matchCompanyToBlueprint(company,demoExecutiveBlueprint);return <SectionCard><div className="flex justify-between gap-3"><h2 className="text-xl font-semibold">Blueprint company fit</h2><StatusBadge tone={a.conflicts.length?"warning":"info"}>{a.score}%</StatusBadge></div><dl className="mt-4 text-sm"><div className="py-2"><dt className="text-slate-500">Aligned dimensions</dt><dd>{a.preferred.join(", ")||"More evidence required"}</dd></div><div className="py-2"><dt className="text-slate-500">Identified conflicts</dt><dd>{a.conflicts.join(", ")||"None identified"}</dd></div><div className="py-2"><dt className="text-slate-500">Evaluated dimensions</dt><dd>Company size, industry, ownership, geography, and culture</dd></div></dl><Link href="/blueprint" className="mt-3 inline-block text-sm text-blue-300">Review Blueprint →</Link></SectionCard>}
