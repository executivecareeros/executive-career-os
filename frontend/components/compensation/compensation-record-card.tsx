import type { CompensationRecord } from "@/types/compensation";import { formatCurrency,formatCurrencyRange } from "@/lib/currency-utils";import { StatusBadge } from "@/components/status-badge";
export function CompensationRecordCard({record}:{record:CompensationRecord}){const amount=record.baseSalaryExact!==undefined?formatCurrency(record.baseSalaryExact,record.currency):formatCurrencyRange(record.baseSalaryMin,record.baseSalaryMax,record.currency);return <article className="rounded-xl border border-white/10 bg-slate-950/40 p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-xs text-slate-500">{record.recordedAt} · {record.source}</p><h3 className="mt-1 font-semibold">{record.recordType}</h3></div><StatusBadge tone={record.disclosedOrEstimated==="Undisclosed"?"warning":"info"}>{record.disclosedOrEstimated}</StatusBadge></div><p className="mt-4 text-2xl font-semibold" aria-label={`${record.recordType}: ${amount}`}>{amount}</p><p className="mt-2 text-sm text-slate-400">{record.paymentFrequency} · {record.grossOrNet} · Confidence {record.sourceConfidence}/100</p><p className="mt-3 text-sm text-slate-500">{record.notes}</p></article>}
