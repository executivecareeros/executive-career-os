import type { CompensationRecord } from "@/types/compensation";import { CompensationRecordCard } from "./compensation-record-card";
export function CompensationTimeline({records,title="Compensation Timeline"}:{records:CompensationRecord[];title?:string}){const sorted=[...records].sort((a,b)=>a.recordedAt.localeCompare(b.recordedAt));return <section aria-labelledby={`compensation-${title.replaceAll(" ","-").toLowerCase()}`}><h2 id={`compensation-${title.replaceAll(" ","-").toLowerCase()}`} className="text-lg font-semibold text-white">{title}</h2>{sorted.length?<div className="mt-4 space-y-4">{sorted.map((record)=><CompensationRecordCard key={record.id} record={record}/>)}</div>:<p className="mt-3 text-sm text-slate-500">No fictional compensation records are linked.</p>}</section>}
