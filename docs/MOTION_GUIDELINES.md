# Motion Guidelines
> Purpose: Keep motion calm, useful, and accessible.

Page entry uses a 220ms opacity and four-pixel translation. Surface changes use 200ms color transitions. Motion never blocks input or communicates facts by itself. `prefers-reduced-motion` disables page entry and skeleton pulsing. No parallax, decorative looping, or playful animation is used.
