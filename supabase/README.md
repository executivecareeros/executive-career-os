# Local Supabase Schema

This directory contains reviewable migrations and fictional seed data. Install the Supabase CLI separately, then run `supabase start`, `supabase db reset`, and `npm --prefix frontend run validate:database`.

The CLI was not bundled with the application. Never place service-role keys, database passwords, or personal identifiers in this directory.
